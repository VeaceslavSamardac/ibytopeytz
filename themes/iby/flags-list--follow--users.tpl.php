<?php
if($flags) {
  foreach($flags as $user) {
    //echo "Show1 ".$flag->flag_type." ".$flag->flag_group." ".$flag->entity_type." with id: ".$flag->entity_id."<br />";
    //echo theme('user_profile', $variables);//, $user);
  }
}
?>
